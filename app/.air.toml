root = "."

tmp_dir = "tmp"

[build]
cmd = "go build -o tmp/main ./cmd/app"
bin = "tmp/main"
full_bin = "tmp/main"
exclude_dir = ["tmp"]


[log]
  time = true

[color]
  main = "yellow"
  watcher = "cyan"
  build = "green"
  runner = "magenta"
